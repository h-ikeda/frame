version: 2
jobs:
    build:
        parallelism: 2
        docker:
            - image: circleci/node:latest-browsers
        steps:
            - checkout
            - run:
                command:
                    yarn
            - run:
                command:
                    yarn test